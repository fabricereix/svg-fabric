Requirements
Python3 with PyYAML and Jinja2


# Update Spec SVG
python3 spec2json.py <svg.yaml | jq . >svg.json



# Extract info from spec
# List attributes for a specific element
$ cat svg.json | jq '.elements[] | select(.name | contains("svg")) | .attributes[].name'
"width"
"height"
"viewport"

$ cat svg.json | jq '.elements[] | select(.name | contains("rect")) | .attributes[].name'
"x"
"y"
"width"
"height"
"fill"

